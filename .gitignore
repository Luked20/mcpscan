node_modules
dist
*.sarif

# Only docs/rules/ is published — it is product documentation, and every
# finding's helpUri links to it (see src/core/help.ts). Everything else under
# docs/ is internal. Written as "ignore all, un-ignore rules/" so a new
# internal document is excluded by default rather than published by accident.
docs/*
!docs/rules/
